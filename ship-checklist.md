# Anchor — Ship Checklist (v1)

Decisions to lock in first, since they change what "done" means for everything below.

- [ ] **Zoom account model**: per-school (school's Zoom admin installs the S2S app under their Business/Education account — email matching + Dashboard data just works) vs. per-teacher on any plan (broader reach, but verified-email matching will never work for teachers on Basic/Pro — name-matching + manual override becomes the primary mechanism, not a fallback)
  - **Proposed (not decided): per-school, for the pilot.** It is the branch where identity matching works properly, the participant scopes exist, §7 stays optional rather than load-bearing, and there is no Zoom review at all — confirmed 2026-08-18, all three Marketplace apps work on Development credentials under the internal-user route. The cost is needing an admin conversation, but a design partner is required anyway and that *is* the admin conversation. Per-teacher is the better long-term growth story; it is a much bigger build, and it makes the bot a prerequisite rather than a differentiator. **Left `[ ]` on purpose — this is a call to make, not one Claude has made.**
  - Still open, but the price of the per-teacher branch is now measured rather than assumed. Confirmed 2026-08-17: the two participant scopes (`dashboard:read:list_meeting_participants:admin`, `report:read:list_meeting_participants:admin`) are not on the app and cannot be added from an account below Business/Education/Enterprise — the Marketplace scope picker shows no Dashboard or Report category to select them from. So on a per-teacher pilot the REST path never returns a participant list at all, and the Meeting SDK bot is not the richer of two sources but the only source of live engagement signal. Picking per-teacher means the bot (§5, §7) is a hard dependency of shipping, not a differentiator on top of it.
- [ ] **Distribution channel**: direct download (Developer ID + notarization) vs. Mac App Store — see reasoning below, but pick one before building the packaging pipeline
- [ ] **Pricing model**: free, per-teacher subscription, per-school license — affects whether you need StoreKit (App Store) or your own payment integration (direct)

---

## 1. Legal & compliance — do this early, it gates everything else

- [x] **Privacy policy — live and substantive**, verified 2026-08-18 at https://anchorteach.vercel.app/privacy (200, prerendered; a bogus path returns a real 404, so these are routes rather than an SPA shell). Thirteen sections. **But see the deploy gap below — the live copy is not the one in this repo.**
- [x] **Terms of service — live**, verified 2026-08-18 at https://anchorteach.vercel.app/terms. `/support` and `/pilot-terms` are live too; `/support` is what the app's new **Get Help** button opens, and the address it prints matches `SupportContact.email`.
- [~] **Written data-retention policy — written, but the public copy still describes the old behaviour.** The mechanism shipped 2026-08-17 (`SessionArchive` + `RetentionWindow`, one term / 120 days by default) and `privacy.tsx` in this repo states it in plain language in two places. **The deployed policy does not.** Fetched 2026-08-18, the live page contains no "120", no "one term", no "school year" — it still says session history is kept "until you delete it". So the published document a school's reviewer reads now *contradicts the shipped app*, and in the direction that matters: it promises data is kept while the app silently deletes it at 120 days. That is worse than the line being merely undone. See the deploy gap below.
  - **Now tested, 2026-08-18 — it was not before, and it is a published legal claim.** `RetentionPolicyTests` pins the windows (120 / 365 / unbounded), that the shipped default is bounded at all, the cutoff arithmetic at its 119/120/121-day boundary, and that each policy sentence quotes its own number. The one that matters is the **cross-artifact pin**: it reads `privacy.tsx` and asserts the site quotes the same window the code enforces — the drift that actually happened.
  - **That test shipped wrong on its first attempt, which is worth recording.** A bare `contains("120 days")` passed the canary it should have failed, because the policy states the number *twice* and the canary changed one. It now matches every `N days after the/each class` occurrence and requires them all to equal the enum, so a half-finished edit fails with `[90, 120]` instead of passing quietly. Checking a guard is non-vacuous is the step that caught it.
  - Still **not** covered: `pruneExpiredSessions` itself. `SessionArchive.sessions` is `private(set)` and seeding it needs the whole begin/update/finalize flow, so the actual deletion is exercised only by the manual pass in §9. The policy is pinned; the mechanics are not.
- [x] **FERPA posture documented** — §8 of the privacy policy, "Student data, FERPA and children", verified live 2026-08-18. It takes the position directly rather than hedging: education records stay under the school's control, Anchor is a tool operated by a school official (the teacher) under the school's direct control, and because student information is never transmitted, Anchor never becomes a recipient or holder of education records. COPPA is answered on the same basis — operator obligations attach to collection *by the operator*, and there is none — while explicitly not relieving the school of its own parental-notice duties. That last clause is the one that makes this credible to a reviewer rather than self-serving.
- [x] **Zoom's developer terms reviewed 2026-08-18 — and they constrain §11, not §7.** The provision that matters is **§3.2.9 of the Zoom API License and Terms of Use**, "Artificial Intelligence and Machine Learning Limitations": *"You may not use any Customer Content … to train, develop, evaluate, test, improve, or modify artificial intelligence or machine learning models (including language models) … except with Zoom's express written permission."* There is one exception, and it is narrow: with a Zoom Account Owner's explicit consent you may use their Customer Content for AI training *"specifically for their use"*, and *"the resulting customized model may only be used by the Customer who consented … and not by any other party."*
  - **What this costs.** §11's plan is to export training rows from live sessions and retrain on real labels. Those rows are built from Zoom participant state and chat — Customer Content reached through Zoom's platform. So the shared-model flywheel is not available: with a partner's written consent their data may improve *their* model only, and that model cannot ship to anyone else. Retraining a single model on pooled pilot data and shipping it to the next school is the specific thing §3.2.9 forbids.
  - **What it does not block.** Google Classroom columns are not Zoom Customer Content and are unaffected. Nor is *scoring* — inference on a shipped model is not training. And the labels are the teacher's own judgement rather than Zoom content, though the feature rows beside them are not, so that does not rescue the export.
  - **Get express written permission before the first retrain, or scope the retrain per-partner.** This also gives the "data flywheel" pitch a second and harder reason not to be made: it was already inconsistent with the on-device privacy claim, and it is now also against Zoom's terms.
  - Not found in that document, and worth knowing rather than assuming: no explicit retention limit on Customer Content, no participant-notice requirement for accessing chat or transcripts, and no stated deletion timetable. Anchor's visible bot and 120-day window are its own choices, not compliance floors.
- [x] **Homepage is live** at https://anchorteach.vercel.app (verified 2026-08-18). Still on a `vercel.app` subdomain — the domain purchase is its own line and remains open.

- [x] **Landing site deployed 2026-08-18 — the published policy now matches the shipped app.** Live at anchorteach.vercel.app: "Last updated **August 17**" (was August 13), *"Session history — deleted automatically **120 days** after each class ends"* (was "kept until you delete it"), and the dropped-email-scope paragraph is live too — *"Anchor does not request your students' email addresses."* Deployed with `npx vercel deploy --prod`; the CLI was already authenticated.
  - **The branch was never the cause, and this is the part worth remembering.** `anchor-landing` has **no Git connection** — the Vercel dashboard still shows "Connect Git Repository" — so merging to `app-split` deployed nothing, and no future merge will either. The Aug 13 build was simply the last time someone ran the CLI. **This will go stale again the next time the policy changes.**
  - **Check the production alias, never the deployment URL.** `anchor-landing-<hash>.vercel.app` returns ~478KB of Vercel SSO login page rather than the site, because deployment-specific URLs sit behind Deployment Protection — the same trap ZOOM_INTEGRATION.md §2a records for the bounce page. It reads exactly like a broken deploy and is not one.
  - **Connected to Git 2026-08-18, so this stops recurring.** `anchor-landing` → `rishabreddy0809/Anchor_MacOS`, Root Directory `website/landing`, Production branch **`app-split`**.
  - **The production branch was the trap.** Vercel defaulted it to `main`, and this repo has no `main` — the default branch is `app-split`. Connecting the repo and stopping there would have looked completely successful and deployed nothing, ever, which is the same failure as before with a green checkmark on top.
  - `anchor-oauth-bounce` still has no Git connection. Not urgent — that page has not changed since Aug 9 and `Web/deploy.sh` covers it — but it is the same trap left armed.

## 2. Google OAuth verification — settled, and it turned out not to be a clock at all

This section was written assuming the longest external dependency in the project. It stopped being one on 2026-08-17, and the heading is left in place only so the reasoning is findable.

- [x] **Consent screen is in Production, and no verification is needed** — settled 2026-08-17. Dropping the `classroom.profile.emails` scope (`044e81e`) is what did it: with that gone, nothing Anchor requests is restricted, and the app publishes to Production without review. The four Classroom scopes it still asks for are in `GoogleOAuthConfig.classroomScopes`, plus `userinfo.email` for identity. **Not re-verified here** — the evidence is the console state at the time, recorded in the comment above that constant; this line records the decision rather than re-testing it.
- [x] **Privacy policy and ToS URLs exist and are live** — see §1. They were needed for the submission that no longer has to happen, and are still needed by a partner's reviewer.
- [x] ~~Expect 2–6 weeks~~ — **does not apply.** The 2–6 week clock was the reason this section said "start immediately"; there is no queue to sit in. This is the single largest schedule item that came off the critical path.
- [ ] **Know what it cost, because this is a live product constraint, not a closed one.** Google no longer returns roster email addresses, so `ClassroomStudent.matchKey` is nil on every entry and identity matching runs on normalised display names. That path refuses ambiguous names outright rather than guessing — two students who normalise alike both drop out — so **a class with two Emmas shows both as unmatched until the teacher links them by hand**. It was the fallback; it is now the ordinary path. Watch this in the first real class: it is the most likely way a pilot roster looks broken, and it is by design.

## 3. Zoom Marketplace readiness

- [x] **Rename the Marketplace app** — done 2026-08-17. The consent screen quotes the app name verbatim and read *"General app 392 would like permission to:"*; it now reads *"Anchor would like permission to:"*. Edited on App Listing → App Name, not the pencil beside the header. Re-check this on any second app created for production, which starts life with a generated name again.
- [x] **Checked in the Marketplace console 2026-08-18 — and the answer is that this line should not be actioned as written.**
  - **`Anchor Bot OAuth` (Server-to-Server): Activated, "Live on your account."**
  - **`Anchor` (browser sign-in): Draft, but "Active for internal users"** — the state §2a says is sufficient. "Draft" reads alarming and is not.
  - **`Anchor Meeting SDK`: also fine.** Its Local Test page offers **Remove App**, not *Add App*, so it is already installed on the account; and Embed → Meeting SDK is on with **"programmatic join use case"** enabled, which is precisely what the bot needs. Its Development Client ID `QdI3h9EXTtWgQWmZB9frVQ` matches the shipped `OAuthClientDefaults.meetingSDKKey`.
  - **Why "activate for production" is the wrong instruction.** I read the blank activation state as a gap; it is not. On the Production tab the app has a *different* Client ID (`wwMAfgtuRiOMTd…`), empty developer contact, and both **Beta Test** and **Publish** marked *Not ready* — because Production here means **Marketplace publication**. §2a already records the price: a complete App Listing, Privacy Policy / Terms / Support URLs, and Zoom's own wording that *"submission of supporting security evidence is mandatory for beta testing"*, budgeted well beyond the 3–4 days the docs quote. **That is the review queue the whole partner strategy exists to avoid** — an app created under the partner's Zoom account makes every teacher there an internal user and skips all of it.
  - **So: leave all three on Development, and do not publish.** What *would* be required if cross-account distribution ever becomes the plan is the full Beta Test path above, plus swapping the shipped `meetingSDKKey` to the Production ID — which activation alone would not do.
- [x] **Per-teacher "Connect your Zoom account" OAuth flow — already built, this line was stale.** `ZoomOAuthHandler.authorize(config:)` and `ZoomViewModel.connectAccount()` are the browser sign-in, on Zoom's General app type rather than Server-to-Server, mirroring the Google pattern as the line asked. PKCE throughout, tokens in the Keychain, and the redirect runs over the hosted bounce page because Zoom rejects loopback (see `ZOOM_INTEGRATION.md` §2a). `ZoomRedirectTransportTests` drives the callback leg over a real socket. **What is still untested is the one click** — no teacher has ever completed this flow end to end, which is the same open item as the fresh-install test, not a gap in the code.
- [ ] If going per-school model: document the admin install process clearly, since a school's Zoom admin is doing this, not the end-user teacher

## 4. Credential distribution — get this out of Settings entirely

- [ ] Ship your own Zoom Meeting SDK key + Google OAuth client ID/secret baked into the app binary (both are already designed to be non-confidential per your own code's PKCE reasoning — don't make users paste keys)
- [x] **Settings is a single Connect button per service in a shipped build — done 2026-08-18.** Both "Advanced" disclosures (Zoom OAuth + Server-to-Server, and the Google OAuth client) are now `#if DEBUG`. A teacher sees Connect and nothing else. **Release build verified**, not just Debug — the point of the change is the configuration a teacher gets.
  - **What made this safe to do was documentation, not the click.** Neither integration needs a secret to sign a teacher in: Zoom runs as a public client, and Google lists `client_secret` as *optional* for installed apps, taking the PKCE `code_verifier` instead. So a shipped build has nothing left to ask for, and a field asking anyway invites a teacher to believe something is missing.
  - `#if DEBUG` rather than deleted, deliberately: it is still the fastest way to point a development build at another registration, and a managed deployment provisions through the `ANCHOR_*` environment variables on first launch, a path this never touched.
  - Three wrong strings fixed on the way. Two told a teacher to "Add one under Advanced" — a disclosure a shipped build no longer renders. The third said to choose synced classes "under Advanced" when course selection has always been in the panel body. And the Google help text asserted that "Google requires the secret on the token exchange even for desktop clients", which is the opposite of what Google documents.
- [x] **Confirm no secrets ship** — verified 2026-08-18, and this line was stale: `BotCredentials.swift` no longer exists anywhere in the repo. What ships is `OAuthClientDefaults`, and all three secret fields are empty strings (`zoomClientSecret`, `meetingSDKSecret`, `googleClientSecret`). Only the Zoom client ID, the Meeting SDK key and the Google client ID carry values, and none of those is confidential — they are public identifiers for a PKCE native client, which is the whole reason the flow uses PKCE. A source-wide scan for assigned secret-shaped literals returns nothing.

## 5. Known bugs to fix

- [x] `PKCE.makeCodeVerifier()` discarded the `SecRandomCopyBytes` status and used the buffer regardless — fixed 2026-08-17, it now checks the status and traps. A silent RNG failure would have handed out a predictable verifier while every other part of the flow looked correct, which is the kind of thing that never shows up in a successful sign-in test.
- [x] **Bot appears as a scored "student" in its own roster** — already fixed; this line was stale. `ZoomMeetingSDKBridge` sets `isSelf: info.isMySelf()` and `MeetingRoles.isBot` checks it first, ahead of the host check, so the bot is filtered even in a meeting it happens to host. REST reports no `isSelf`, so on that path the bot is matched by the name it joined under — as a prefix, to survive the suffix Zoom appends when a display name is already taken. Pinned by `MeetingRolesTests` on 2026-08-17. One trap left in place deliberately: `botName` is prefix-matched, so a deployment overriding the default with something short like "Anchor" *would* claim a student called "Anchor Patel" and drop them from the dashboard with nothing on screen to say so. The shipped default ("Anchor (engagement assistant)") is long and parenthesised precisely so that stays hypothetical — anything replacing it has to be too.
- [~] Reconnection handling: **the arithmetic is now verified; the live rehearsal is not.** Split out of `ZoomViewModel` into `PollSchedule` and covered by `PollScheduleTests` on 2026-08-17 — the backoff ladder, its ceiling, jitter bounds, and which failures keep polling versus stop and ask for the teacher. Two defects fixed on the way: (1) `rateLimited(retryAfter:)` had carried Zoom's own `Retry-After` since it was introduced and **nothing ever read it**, so a 429 saying "wait two minutes" was answered fifteen seconds later — earning another 429 and walking the ladder up exactly when the loop needed to stop; it is now taken as a floor against the ladder, capped so a bogus value can't park a lesson past the bell. (2) The wait loop re-drew the random jitter on every 500ms slice, so the "retrying in Ns" shown to the teacher was an independent sample from the wait actually in progress, and the jitter collapsed toward its base instead of decorrelating clients; it is now drawn once per cycle. Laptop sleep needs no handling and gets none — the loop measures against a wall clock, so a lid closed for an hour comes back with the interval long exceeded and syncs at once, which is what a teacher reopening mid-lesson wants. **Still open:** the actual manual pass — drop a real meeting, sleep a real laptop, pull real Wi-Fi — since none of the above exercises the loop, only the numbers it consults.
- [ ] Multi-participant scale: validate UI, polling cadence, and Classroom sync cost with a real class size (20–30 students), not just 2–3 known test accounts

## 6. Product polish for real (non-you) users

- [~] **First-run onboarding flow — built, never watched on a fresh install.** The line this replaces said it "assumes a technical user who understands OAuth client IDs"; that stopped being true when `OnboardingView` landed. Six steps (welcome → name → Zoom → Classroom → preferences → done), presented as a sheet from `MainWindowView` on first launch and replayable from Settings, and **it never asks for a credential**: Zoom and Google each get a full screen explaining *why* before the sign-in, with the connect button gated on `hasClientCredentials` / `hasClientID`, both of which need only the client ID — and both client IDs ship in `OAuthClientDefaults`. So on a teacher's Mac the buttons are live, not greyed out with a pointer to Settings → Advanced.
  - **Still open, and it is the whole item:** nobody has run this on a machine that has never had Anchor on it. Everything above is read off the source, not off a screen.
  - **The one thing to check first, because it fails late.** Both client *secrets* are empty in source by design, and the only ways in — Settings → Advanced, or the `ANCHOR_*` environment variables — are developer-machine paths that a teacher's fresh install has neither of. Zoom is believed fine: `ZoomOAuthHandler` omits the Basic header and sends `client_id` in the body when no secret is present, and ZOOM_INTEGRATION.md §2a records **Use Public Client OAuth** as enabled, which is exactly that path — but it is the same untested leg as the Connect click, and the Production app is a separate registration where the toggle has to be enabled again. **Google resolved on the documentation, 2026-08-18 — the concern was mine, and it was wrong.** Google's own "OAuth 2.0 for iOS & Desktop Apps" lists the token-exchange required parameters as `client_id`, `code`, `code_verifier`, `grant_type` and `redirect_uri`, with **`client_secret` explicitly Optional** for installed applications; PKCE is the mechanism that stands in for it, on the stated reasoning that installed apps "cannot keep secrets". `GoogleOAuth` sends `code_verifier` and omits the secret when absent, which is exactly that contract. So a fresh install should connect. Residual risk is small but not zero — "documented optional" is not "observed on this client", and `client_secret is missing` does occur in the wild for *Web application* client types — so the one click still confirms it rather than settling it. It is no longer the thing most likely to break a first run.
- [~] Simplify meeting join to accept a pasted Zoom link — **built and tested 2026-08-17**, but not yet seen on screen. `ZoomMeetingLink` parses the whole invitation a calendar sends: join URLs (`/j/`, `/w/`, `/s/`, vanity subdomains, `zoommtg://`), the grouped "Meeting ID: 812 3456 7890" label, a bare number typed by hand, and both passcode spellings. It collapses the paste into the two fields as it lands, so the teacher sees what Anchor understood before they join. Two decisions worth knowing: it *never* scans free text for a plausible digit run, because a Zoom invitation's dial-in block contains "+1 312 626 6799" — eleven digits stripped of punctuation, exactly a personal-meeting-id length — so digits are only read from somewhere that names them; and where an invitation carries both passcodes, the labelled one wins over the URL's `pwd=`, since the latter is encoded for the browser while `ZoomSDKJoinMeetingElements.password` wants what a human types. A personal-room link (`zoom.us/my/…`) carries no id at all and now says so in the panel instead of failing deep in the SDK. **Still open:** actually pasting into the running app — 19 parser tests pass and it compiles, but the field behaviour has not been watched by a human.
- [x] **Empty states and error states reviewed for plain-language clarity — done 2026-08-18** (`a1e8bdb`). The empty states were already fine: every one names what is happening, and most carry an action. The *error* strings were not. They were written by the person who also wrote the integrations, so the setup ones read as instructions to that person — "add the scope to your Server-to-Server OAuth app, then re-activate it", "create a Meeting SDK app in the Zoom Marketplace", "add the scope in the Google Cloud console under APIs & Services → OAuth consent screen → Data access". A teacher can do none of it, and an instruction implies the reader erred, so a misconfigured build reads as *their* mistake and goes unreported.
  - **The split is audience, not severity.** `.authorizationExpired` and `.noActiveMeeting` are one click for a teacher; a missing scope or an unconfigured SDK key is a property of the build they were handed. `ZoomError` and `ClassroomError` both answer `isSetupProblem` now, and the views branch on it. Google's `.insufficientScope` is deliberately excluded — its consent screen is per-permission and un-ticking a box produces exactly that error, so it keeps a real instruction; Zoom's cannot be granted below a Business plan at all.
  - **The technical sentence moved rather than died.** `technicalDetail` carries it into the support mail, where it reaches the one reader who can act on it.
  - `SupportContactTests` enumerates both enums and fails on developer vocabulary in teacher-facing copy. It caught two strings the first pass missed.
  - **The enum tests gave false confidence, and `TeacherFacingCopyTests` (2026-08-18) is the answer.** They passed while *six* view literals told teachers to do impossible things — and every one was found by hand, while doing something else. Three in `SettingsView`, one in `ConnectionStatusView`, one in `ClassroomConnectionPanel`, and one in `HomeView` that was simply **false**: "Anchor ships no Google credentials of its own", written before the client ID shipped on 2026-08-17 and never revisited. An enum test cannot see a `Text("…")` — there is no value to interrogate — so the new test scans the source instead. **It found the sixth on its first run**, in a file already read twice that day.
  - **That debt is paid, and the exemption is gone.** Both files now gate the credential UI at the *declaration* as well as the call site, so a Release build compiles none of it — the dead views, their state and their strings are absent from the shipped binary. `testResult` and `testSucceeded` deliberately stay outside the gate: `save()` writes them, but so do `connect()` and `disconnect()`, which every teacher uses.
  - **The test now has no file exemptions at all**, which was the point — an exemption is a hole, and would have let a genuinely bad string sit in either file forever. `#if DEBUG` is the only thing that silences the scan now, and that is the honest signal: a string not compiled into the shipped app cannot be read by a teacher. Verified non-vacuous by planting `"Paste your Client Secret from the Marketplace"` in the previously exempt file and watching it fail.
- [ ] App icon and branding assets finalized
- [x] **In-app support/contact path — done 2026-08-18** (`a1e8bdb`). Two entries, deliberately different. Settings → **Get Help** opens the support page, because nothing has gone wrong at that point and the teacher is browsing. A failure instead offers **Email support**, which opens a composer pre-filled with what broke, the error's `technicalDetail`, and the build and macOS version — so a report arrives actionable without a teacher hunting for any of it. `SupportContact` is the single source of the address and is pinned by test against the marketing site's `CONTACT_EMAIL`; the body is composed only from values Anchor supplies, so nothing about a class or a student can reach it.

## 7. The differentiator feature — built, unwatched

**This whole section was stale.** All five lines were open; all five are implemented. Audited 2026-08-18 by reading the code rather than the titles. What has never happened is a real lesson running through it, which is a different item and lives in §9.

- [x] **Live transcript capture** — `ZoomMeetingSDKBridge.startLiveTranscription()` drives `getCloseCaptionController()`, handles the already-started and connecting states, and detects the feature being disabled for the account rather than failing opaquely. It also asks the host to start captions when it cannot start them itself.
- [x] **Topic extraction from the rolling window** — `TranscriptCaptureService` (327 lines) holds the window; `LiveCoachViewModel` runs one pass per Zoom poll, immediately after the scores land, so what is on screen is always about the roster on screen.
- [x] **Cross-reference against Classroom assignment titles** — `TopicMatcher` (365 lines), joined to the academic snapshot at `LiveCoachViewModel.swift:327`, with a `minimumRelevance` floor below which a match is discarded rather than shown weakly.
- [x] **Trigger-gated generation** — and the gate is the feature, exactly as this line intended. Two clocks: scores move every ten seconds, the topic every ten minutes, so extraction fires only when the transcript has *actually moved* **and** a minimum interval has passed. Recommendations are cached on the facts they were built from. Without that cache this would run an inference per student per ten seconds for a whole lesson to re-derive text that had not changed.
- [x] **Surfaced in "Suggested Next Steps", provenance always stated** — `StudentDetailView:206`. One box rather than two: when the model has an answer that *is* the answer, and the composed lines would restate it worse; when it doesn't (no Apple Intelligence, no transcript, a refusal, a timeout) `RecommendationGenerator.fallback` is the whole feature. Either way the source is labelled, on the stated principle that a teacher acting on a sentence about a child should know whether a model wrote it.
- [ ] **Watch it run in a real lesson.** None of the above has met a live class. This is the §7 item that is actually open, and it rides along with the three live sessions in §9 rather than being scheduled separately.

## 8. Distribution mechanics

**Either way:**
- [x] Deployment target lowered to **macOS 14.0** (was 26.5) on 2026-08-17. The 26.5 floor existed solely because `FoundationModelAnalyzer` referenced FoundationModels unconditionally — one optional phrasing feature setting the hardware requirement for the whole product, which for a pilot audience of teachers on school-issued Macs meant almost nobody could install it. FoundationModels is now behind `@available(macOS 26.0, *)` with `#available` at its three entry points, and the vendored Zoom SDK only ever asked for 10.15. 13.0 remains blocked by SwiftUI APIs newer than it — `ContentUnavailableView`, `SettingsLink`, the two-argument `onChange`, `symbolEffect` and `variableColor` need 14.0, and `scrollBounceBehavior` needs 13.3 — so dropping further is a UI rewrite rather than a build setting, and 14.0 is the floor unless that becomes worth doing.

**If direct download:**
- [ ] Enroll in Apple Developer Program ($99/yr)
- [ ] Sign with Developer ID Application certificate, hardened runtime enabled
- [ ] Notarize via `notarytool`, staple the ticket
- [ ] Package as a `.dmg`
- [ ] Host the download (your own site or GitHub Releases)
- [ ] **Set up Sparkle for auto-updates — blocked on three things, none of them code.** Assessed 2026-08-18 and recorded because it reads like unblocked engineering work and is not. It needs (1) an appcast URL to point at, which waits on the domain; (2) an EdDSA key pair whose private half only you can generate and hold; and (3) a signed, notarized build to actually update *to*. The project also carries **no SPM dependencies at all** — the Zoom SDK is vendored — so adding Sparkle sets a precedent for how third-party code enters this project, which is a decision rather than a step. Writing the integration before the URL and key exist produces something inert that cannot be tested and gets reworked once they do. Sequence it after the Developer ID certificate.

**If App Store:**
- [ ] Enroll in Apple Developer Program ($99/yr)
- [ ] App Store Connect listing: screenshots, description, App Privacy "nutrition label" (this will require you to accurately disclose the Classroom/Zoom data you touch)
- [ ] Review Apple's stance on background-running meeting-observer apps before investing further — this is the single biggest risk of rejection for this category of app, worth a sanity check early rather than after full submission
- [ ] Budget for review cycles on every future update, not just launch

## 9. QA pass before calling it v1

- [x] **Automated coverage exists at all** — an `AnchorTests` XCTest target, **229 passing tests** as of 2026-08-19 (the repo had none before 2026-08-17). Thirteen files, each pinning a layer where a mistake is silent rather than loud:
  - `ScoreShapingTests` (17) and `RiskLevelThresholdTests` (8) — score shaping and the three RiskLevel cut-offs.
  - `AcademicEscalationTests` (29) — the hand-written academic rules, and the +0.20 bound on them.
  - `FeatureCalculatorTests` (68) — the 16-feature vector and the cross-poll accumulators that build it.
  - `MeetingRolesTests` (18) — the roster gate that decides who is scored at all.
  - `ZoomRedirectTransportTests` (16) — the redirect transports, driven over a real loopback socket.
  - `ZoomMeetingLinkTests` (19) — the invitation parser.
  - `PollScheduleTests` (18) — the backoff ladder, its ceiling, jitter bounds, and which failures stop polling.
  - `ModelRoutingTests` (13) — which of the two models a given student is scored by, and separately whether that model may switch `AcademicEscalation` off. Those are different questions; the five added on 2026-08-19 exist because they had been answered by one flag (§11).
  - `SupportContactTests` (8) — that no teacher-facing error string uses developer vocabulary, and that the technical sentence survives into the support mail.
  - `ReleaseHygieneTests` (2) — that nothing logs to the console outside `#if DEBUG`.
  - `TeacherFacingCopyTests` (2) — that no view literal tells a teacher to do something only a developer can.
  - `RetentionPolicyTests` (7) — the retention window, and that the marketing site quotes the same number the code enforces.
  - **Eleven real defects across those passes**, which is the argument for having written them: three in the feature vector; two in the redirect transports; two in `PollSchedule` (a `Retry-After` that nothing read, and jitter re-drawn every 500ms); the PKCE entropy status discarded; a hand-raise penalising the student holding it up; a student's name printed into the release log; and two error strings still speaking to a developer. The roster gate turned out to be already correct and is simply pinned. See the commit messages for what and why.
  - It still says nothing about the manual passes below, which all stay open. **Every test here runs against code, never against Zoom, Google or a real class** — which is exactly why the manual lines cannot be closed by adding more of these.
- [~] **The four manual passes are scripted but not run — `QA-PROTOCOL.md`, written 2026-08-19.** `[~]` and not `[x]`: a protocol is not a pass, and none of these four lines moves until someone runs it on a borrowed Mac with a real class. What it removes is the improvisation — a borrowed Mac and a real class are both scarce, and the failure mode is getting one and realising afterwards that the interesting half was never exercised.
  - **The finding that makes it worth having: deleting Anchor does not produce a fresh install.** Four Keychain services (`com.anchor.zoom.credentials`, `.zoom.oauth`, `.zoom.meetingsdk`, `.google.classroom`) and the `Rishab-Reddy.Anchor` defaults domain all survive dragging the app to the Trash. A fresh-install walkthrough on this Mac without clearing them tests a path no pilot user will ever walk, and would have looked like a pass. §0 of the protocol has the commands and the confirmation checks.
  - **Two things the protocol adds that were not on anyone's list.** Google presents the four Classroom scopes as *individual tick boxes* and grants only what is ticked, so a teacher ticking three of four is a likely case, not a hypothetical — the protocol runs it deliberately. And a class with two names that normalise alike shows *both* students as unmatched by design, so `ManualRosterLinks` is the recovery path for every real-roster collision and has never been exercised on a real name.
  - **Pass A is blocked by Apple Developer enrollment and fails by construction until it lands.** An unnotarised build is refused on a Mac that has never seen this developer with *"Anchor is damaged and can't be opened"* — a lie about the binary that reads as one. Working around it with right-click-Open invalidates the pass, because no teacher will do that.
  - [ ] Pass A — fresh-install onboarding walkthrough, done by someone who isn't you
  - [ ] Pass B — full session, start to finish, with a real unfamiliar class size
  - [ ] Pass C — network interruption recovery
  - [ ] Pass D — classroom disconnect/reconnect flow
- [x] **No dev credentials, test accounts or debug logging in the release build — audited 2026-08-18** (`1c86934`). Credentials were already clean (§4). Test accounts are clean: the only addresses in source are `DemoData`'s, all on the reserved `example.edu` domain and behind the `ANCHOR_DEMO_DATA` gate. **Debug logging was not clean, and this is what the line was for.** `ZoomMeetingSDKBridge` carried sixteen bare `print("ANCHOR-DIAG …")` calls and not one `#if DEBUG` — in Release those go to the unified log, readable with Console.app — and one fired on every chat message printing `chat.senderName`. A student's name, logged off the app, in a build handed to a teacher. All sixteen now route through `AnchorDiag.log`, which wraps `print` and `Logger` in `#if DEBUG`.
  - `AnchorDiag` had stated this policy in its own header all along; the bridge just never imported it. A policy a file can opt out of by not using it is not a policy, so `ReleaseHygieneTests` now asserts it across the target — no `print`/`NSLog`/`debugPrint` outside `#if DEBUG`, tracking `#if` nesting and flipping on `#else`. Checked non-vacuous against a planted call before being trusted.
- [~] Run `scripts/verify-no-demo-data.sh` against the exact build you are shipping — **passes as of 2026-08-18** against the current Release build: no demo reference reachable from a Release compile, Release build confirmed (no debug dylib), no demo symbols in the symbol table, none of the 36 long fabricated literals present. Left at `[~]` rather than `[x]` on purpose: the guarantee is per-binary, so this has to be re-run against the actual signed artifact you ship, not against whatever is in DerivedData today.

## 10. Launch readiness

- [~] **Support channel — live and reachable from inside the app; "monitored" is a standing commitment, not a shippable artefact.** As of 2026-08-18 the outbound half is done: `/support` is live and prerendered, Settings → **Get Help** opens it, a failure opens a pre-filled report, and `SupportContactTests` pins the app's address against the site's `CONTACT_EMAIL` so the two cannot drift apart silently. A teacher who hits a wall can now reach a human without leaving Anchor.
  - **The inbound half is still broken, and it is the conversion path rather than the support one.** `submitPilotApplication` falls back to `onboarding@resend.dev`, which delivers only to the Resend account owner, so a pilot application from a stranger fails or lands in spam. That needs `PILOT_FROM_EMAIL` and a verified domain in Vercel production — it is on the blocked-on-a-human list and is the cheapest item on it.
  - Left `[~]` because the remaining word is "monitored". Nothing in a repo can discharge that; it is a promise the support page already makes in your name ("there is no ticketing system and no bot in front of it"), so the only thing that closes this line is deciding you will actually read it daily during the pilot.
- [x] **Versioning and release process written down — 2026-08-18, in `RELEASE.md`.** Written while packaging was fresh rather than reconstructed in October, which is the whole point of the line. Covers the two version numbers and why they answer different questions (`MARKETING_VERSION` is what a teacher reads; `CURRENT_PROJECT_VERSION` is a monotonic integer Sparkle compares — reuse one and a teacher mid-term is silently never offered the fix you shipped for them), the five release commands, and the fact that both numbers appear in **four** build configurations, so changing one leaves Debug and Release disagreeing in a way that only shows up in a shipped artifact.
  - Versions bumped to **0.9.0 / build 2**. Not 1.0: nobody has installed this, and the accuracy behind its core claim has never been established on real labels — the site says so explicitly, so the version should not say otherwise. Going 1.0 → 0.9 was only safe because no build exists in anyone's hands; it stops being safe the moment one does.
- [x] **Rollback plan written — `RELEASE.md`.** The mechanism is: keep every shipped `.dmg` permanently and point the teacher at the previous one. There is no server-side switch and no staged rollout, so recovery is *downgrade*, not hotfix — a teacher mid-semester cannot wait for a fix. Two things make it ugly and are recorded now rather than discovered later: **Sparkle will offer to update them straight back onto the broken build**, since its build number is higher, so a rollback means pulling the appcast entry too; and `SessionArchive` is read by whichever version runs, so a downgrade is only safe while the archive format is unchanged — a release that changes it must stay backwards-readable or say plainly in its notes that rolling back loses history.

## 11. Reach and accuracy, once real sessions have run

Both of these are gated on things that don't exist yet — a partner's LMS instance, and labelled rows from live classes — so neither can be started early by working harder. Written down now so the shape of each is settled before the day it becomes possible.

- [~] **Canvas dependency test — desk half done 2026-08-17, live half waiting on a partner.** Written up in `CANVAS_SPIKE.md`; runnable via `scripts/canvas-dependency-test.py`. On paper Canvas answers yes to everything: all five `ClassroomDataProviding` methods map to documented endpoints, the bulk submissions endpoint exists so a class is one paged call rather than N round trips, and all five academic features have a source. Two are *better* than the Google path — Canvas states `late` and `missing` as first-class booleans where `GoogleClassroomService` has to derive both. Still **no Canvas code in the repo**, deliberately: the connector decision waits on the live run.
  - **The pass/fail criterion is identity matching, not grades**, and it is the one thing the docs cannot settle. `login_id` is documented as always present; `email` and `sis_user_id` are both explicitly permission-gated and configured per institution. So the live run has to happen against the *partner's own* instance, with a token no more privileged than a real deployment's — a sandbox with permissive defaults would answer the wrong question.
  - **The finding worth acting on:** Anchor's Classroom path has no email at all any more (the `classroom.profile.emails` scope was dropped on 2026-08-17, so `AcademicSnapshot.email` is always nil on a normal install) and matches by normalised display name. If `login_id` survives a real install, Canvas identity matching would be **stronger than the Google integration already shipping**, not a compromise. Worth knowing before budgeting three weeks on the opposite assumption. Caveat: `login_id` is a stable key for joining Canvas to itself; whether it joins Canvas to *Zoom* needs one real roster beside one real participant list, which is a partner-call task the script cannot do.
- [x] **Two models, routed per student — shipped 2026-08-18** (`c465a52`, `1e54d88`, `7ed0e05`, `e9a902f`). `StruggleDetectionService` now holds both and picks inside each prediction on `features.observed.contains(.academic)`: per student, per poll, not per launch.
  - `StudentStruggleModel_16` — a student matched in Classroom or Canvas. 81.43% accuracy vs 67.96% baseline, 66.22% recall, ROC AUC 0.8783, 3.84% off the Bayes ceiling. 6/6 scenario + 6/6 direction gates.
  - `StudentStruggleModel_11` — everyone else, engagement columns only. 74.70% vs 68.14% baseline, 47.50% recall, ROC AUC 0.7930. 4/4 scenario + 1/1 direction gates.
  - **Why two and not one:** the academic columns carry benign defaults (`grade_average` 80, `grade_trend` 100) and a 16-feature model cannot tell a default from a measurement — it puts 82% of its weight on those columns, so an unmatched student would read as fine. Why not three: Canvas and Classroom produce the same five columns via `AcademicSnapshot`, so there is no Canvas-specific model to build.
  - **Tuning is finished on this feature set, and that is the result.** The engagement model was retrained on 56,234 rows with a 400-candidate search — four times the data and nearly twice the search of the previous run — and ROC AUC moved 0.7933 → 0.7930. A flat line. The ~10% gap to the Bayes ceiling is irreducible noise, not headroom, because the generator draws an unmatched row's label from a latent term that eleven engagement columns only partly reveal. Recall (46.43% → 47.50%) and the 'obviously struggling' scenario (71.6% → 78.5%) are the two things that did move, and both are the ones worth having. **Do not spend more time on hyperparameters here** — the next real gain is academic coverage or real labels, not search.
  - Still synthetic. This does not touch the item below.
- [x] **Latent gap closed — the stand-down guard now requires the full academic set, 2026-08-19.** Found 2026-08-18 by reading; fixed by narrowing the guard rather than by dropping the model, because reading the code showed the recorded recommendation would not have worked.
  - **The defect.** `StruggleDetectionService.usesAcademicFeatures` was `academicModel != nil`, and `Loaded.isAcademic` is `!academicFeatureNames.isDisjoint(with: declaredInputs)` — **any** academic column, not all five. `StudentStruggleModel_CORRECTED` declares 13 (verified off the compiled model, not the write-up: the 11 engagement columns plus `grade_trend` and `missing_assignments`), so it counted as academic and stood **all five** rules down while blind to `grade_average`, `days_since_submission` and `late_submissions`. `AcademicEscalation:214` escalates on exactly `daysSinceSubmission >= 14` with past-due work, so on that path a student two weeks past due was read by neither the model nor the rules — silent, in the direction this product exists to prevent.
  - **Two corrections to what was recorded on 18 Aug, both found by reading the code rather than the note.** First, **dropping `_CORRECTED` from `candidateResourceNames` would have changed nothing at all.** `candidateModelURLs` appends *every* `.mlmodelc` in the bundle after the named ones, and `Anchor/` is a `PBXFileSystemSynchronizedRootGroup` with no membership exceptions — so a model ships, and loads, purely by existing in that directory. The list is a preference order, not an allow-list. The edit would have looked like a fix, passed every test, shipped the same bytes, and left the gap exactly where it was. Second, **the saving was 3.2 MB, not 7.5 MB**: 7.2 MB is the source `.mlmodel`, and what ships is the compiled `.mlmodelc`. Against a 685 MB app (the Zoom SDK dominates) that is ~0.5%, so size was never an argument for anything.
  - **The fix taken was option 1, not option 3.** `usesAcademicFeatures` is now `academicModel?.readsFullAcademicSet ?? false`, a strict `isSubset` against all five columns. Routing is deliberately left permissive — `isAcademic` still means "reads *any* academic column", so a matched student is still preferred onto the richer model. Splitting those two questions is the whole fix: they are different questions that happened to coincide for every first-choice model Anchor ships, which is why conflating them was invisible.
  - **Why this beats deleting the model.** The guard makes the two layers exhaustive *by construction* rather than by coincidence of which files are in the bundle — whatever a partial model cannot see, the rules still cover, including a partial model shipped by someone else later. It also **defuses the shipping decision rather than pre-empting it**: keeping `_CORRECTED` is now safe, so whether to reclaim its 3.2 MB is an ordinary, non-urgent call for Rishab and no longer a correctness question. The cost is that a partial model double-counts the two columns it *can* see — bounded at +0.20 by `maximumAdjustment`, and in the safe direction, which is the trade the guard chooses on purpose.
  - **Both directions proved non-vacuous before being trusted.** Planting the original `academicModel != nil` fails exactly the two tests that describe the defect; planting the over-correction `return false` — which would double-count academic evidence forever — fails exactly the two that describe the intended stand-down. Five new tests, 220 → **229 passing**, Release builds clean.
  - **This needed a seam, and that is the point worth keeping.** Against the shipped bundle the property *cannot* be caught being wrong: the full 16-feature model is present and wins, so the correct rule and the broken one both answer true. Every pure-function test over column sets would have passed the unfixed code. `StruggleDetectionService(resourceNames:)` loads a chosen model set so the property can be put in front of the real 13-feature file — the case it exists for. A guard that only ever sees the passing case is not a guard.
- [ ] **Retrain the model on real labelled rows.** `retraining/` is set up and its README sets the honesty bar: under 200 rows you can claim *nothing* and the run is a smoke test; 200–1000 tells you whether the model beats the majority-class baseline; 1000+ supports a cautious accuracy comparison with a confidence interval. Everything shipped today was trained on synthetic data, so the first run against real labels is the first honest read this project has had. The scarce resource is labelled *struggling* students rather than rows — a class of 28 with two strugglers yields two useful positives, which is why three sessions is a floor and not a target. Labels have to be the teacher's judgement; deriving them from Anchor's own score just trains the next model to agree with this one.
  - **Know what a successful retrain switches off.** The moment a model declaring all 16 columns is loaded, `StruggleDetectionService.usesAcademicFeatures` turns true and `AcademicEscalation` — the hand-written, teacher-visible, +0.20-bounded academic rules — disables itself automatically with no Swift change. That is the intended design and it is why the 16-feature retrain is worth doing at all, but it means a retrain silently changes the path every academic signal takes to a score. Re-run the suite and read a few real students before trusting the result.
